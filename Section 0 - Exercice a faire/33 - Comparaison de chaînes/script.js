// Objectif : Implémentez une fonction qui compare deux chaînes de caractères et renvoie true si elles sont identiques (respectant la casse), sinon renvoie false.
// Variables : Aucune variable nécessaire.
// Fonction :
// comparerChaines(chaine1, chaine2) : Cette fonction prend deux chaînes de caractères en entrée, les compare caractère par caractère, et renvoie true si elles sont identiques, sinon renvoie false.